import { PlaceholderPage } from '@/components/PlaceholderPage';

export const metadata = { title: 'Entrar — AstroSoma' };

export default function Entrar() {
  return (
    <PlaceholderPage
      titulo="Iniciar sesión"
      nota="El login real (Paso 4 de la Secuencia Maestra) se construye después del onboarding y el paywall, con el método definido en la etapa de autenticación."
    />
  );
}
