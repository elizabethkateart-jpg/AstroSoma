import { PlaceholderPage } from '@/components/PlaceholderPage';

export const metadata = { title: 'Tu plan — AstroSoma' };

export default function Paywall() {
  return (
    <PlaceholderPage
      titulo="Tu plan de liberación"
      nota="El paywall real (Paso 3 de la Secuencia Maestra) se construye en la próxima etapa: aquí vivirán los planes anual y mensual, la garantía y el checkout de Hotmart."
    />
  );
}
